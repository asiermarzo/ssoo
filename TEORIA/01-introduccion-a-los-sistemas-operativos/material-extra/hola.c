/* hola.c — fuente C: se compila a un binario nativo antes de ejecutarse.
 *   gcc hola.c -Wall -Wextra -o hola
 *   ./hola
 */
#include <stdio.h>

int main(void) {
    printf("Hola desde C (binario nativo)\n");
    return 0;
}
